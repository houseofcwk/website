# ── Public (browser-exposed) ────────────────────────────────────────────────
PUBLIC_SITE_URL=https://cwkexperience.com
PUBLIC_CF_ANALYTICS_TOKEN=
# Google Tag Manager — omit to disable tracking (local dev, previews)
PUBLIC_GTM_ID=

# Firebase Web SDK (analytics + events) — omit measurementId to disable.
# Values come from Firebase Console → Project settings → Web app config.
PUBLIC_FIREBASE_API_KEY=
PUBLIC_FIREBASE_AUTH_DOMAIN=
PUBLIC_FIREBASE_PROJECT_ID=
PUBLIC_FIREBASE_STORAGE_BUCKET=
PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
PUBLIC_FIREBASE_APP_ID=
PUBLIC_FIREBASE_MEASUREMENT_ID=

# Mixpanel (event analytics) — omit to disable the Mixpanel adapter
PUBLIC_MIXPANEL_TOKEN=

# Ad pixels — omit IDs to disable each adapter individually
PUBLIC_META_PIXEL_ID=
PUBLIC_TIKTOK_PIXEL_ID=
PUBLIC_LINKEDIN_PARTNER_ID=

# LinkedIn per-conversion IDs (numeric, from Campaign Manager). Optional.
PUBLIC_LINKEDIN_CONVERSION_WAITLIST=
PUBLIC_LINKEDIN_CONVERSION_LC_EMAIL=
PUBLIC_LINKEDIN_CONVERSION_LC_TOUR=
PUBLIC_LINKEDIN_CONVERSION_CONTACT=
PUBLIC_LINKEDIN_CONVERSION_ASSESSMENT_CALL=
PUBLIC_LINKEDIN_CONVERSION_ASSESSMENT_WAITLIST=
PUBLIC_LINKEDIN_CONVERSION_POWERUPS_CHECKOUT=

# ── Cloudflare KV binding ───────────────────────────────────────────────────
# WAITLIST is a KV namespace binding — configured in CF Pages dashboard,
# NOT set as an env var. For local dev, add a binding in wrangler.toml.

# ── Resend (server-side secrets — set via CF Pages dashboard or wrangler) ───
# wrangler pages secret put RESEND_API --project-name=houseofcwk
# wrangler pages secret put FROM_EMAIL  --project-name=houseofcwk
# wrangler pages secret put FROM_NAME   --project-name=houseofcwk
# wrangler pages secret put REPLY_TO_EMAIL --project-name=houseofcwk
# wrangler pages secret put REPLY_TO_NAME  --project-name=houseofcwk
RESEND_API=re_...
FROM_EMAIL=hello@cwkexperience.com
FROM_NAME=CWK. Experience
REPLY_TO_EMAIL=hello@cwkexperience.com
REPLY_TO_NAME=Kris San
