# Local Wrangler dev secrets — copy to .dev.vars and fill in values.
# .dev.vars is git-ignored. Never commit real keys.
#
# Start local dev with: npx wrangler pages dev dist --kv WAITLIST

RESEND_API=re_...
FROM_EMAIL=waitlist@cwkexperience.com
FROM_NAME=CWK. Experience
REPLY_TO_EMAIL=kris@cwkexperience.com
REPLY_TO_NAME=Kris San
